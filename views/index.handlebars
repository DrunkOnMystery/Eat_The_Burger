<h1>Burgers</h1>

<h2>Burgers ready to be eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless eaten}}
      {{> burgers/burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burger-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a new burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger type:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Has this burger been eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> !<br>
    <input type="radio" name="not-eaten" value="0"> Not Eaten!
  </div>

  <button type="submit">Add a new Burger!</button>
</form>